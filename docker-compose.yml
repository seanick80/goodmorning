services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: goodmorning
      POSTGRES_USER: goodmorning
      POSTGRES_PASSWORD: goodmorning
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
